import { components } from './api.types';

export type Request =
  components['schemas']['Request'];

export type Response =
  components['schemas']['Response'];

// TODO decide if we're using InvokeResponse here at all